#!/usr/bin/env bash
# GENERATED by sync-ght-hooks.sh
ROOT="$(git rev-parse --show-toplevel 2>/dev/null)"
[[ "$(git config core.hooksPath 2>/dev/null)" != ".hooks" ]] && git config --local include.path ../.gitconfig
ENFORCE="$ROOT/.hooks/scripts/enforce-git-identity.sh"
[[ -x "$ENFORCE" ]] && "$ENFORCE" "$ROOT" --quiet || true
